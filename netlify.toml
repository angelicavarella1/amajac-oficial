[build]
  command = "npm install && npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "22"

# ESSENCIAL para Vue Router (modo history):
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200